<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- WorkManager's foreground service (GalleryDownloadWorker calls
             setForegroundAsync). Merge the dataSync type onto the library's
             SystemForegroundService declaration so the foreground-service type
             is declared in the manifest (required by androidx.work lint
             SpecifyForegroundServiceType and by Android 14+). -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required by UpdaterPlugin: fires the system install intent for
         the freshly downloaded APK. User grants the permission once
         via system UI on first update attempt. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Downloads use the Storage Access Framework (PublicLibraryPlugin):
         the user picks one folder via ACTION_OPEN_DOCUMENT_TREE and we hold a
         persisted URI permission for just that tree. No all-files access, no
         WRITE_EXTERNAL_STORAGE needed. -->

    <!-- Background download worker (GalleryDownloadWorker via WorkManager):
         it runs as a foreground service so downloads continue when the app is
         backgrounded/killed. On API 34+ the dataSync service type is also
         declared on the ForegroundInfo in code; POST_NOTIFICATIONS (API 33+)
         is required for the visible progress notification.
         NOTE: WorkManager's default initialization (the androidx-startup
         provider) is merged in from the work-runtime library manifest — it is
         intentionally NOT disabled here. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
